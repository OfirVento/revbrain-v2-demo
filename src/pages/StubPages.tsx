// Stub pages for routes not yet built — will be filled in subsequent steps

export function ExecutePhasePage() {
  return (
    <div className="max-w-4xl mx-auto px-6 py-8">
      <h1 className="text-2xl font-semibold mb-2">Execute Phase</h1>
      <p className="text-[hsl(var(--muted-foreground))]">Execute Phase — coming next</p>
    </div>
  );
}
